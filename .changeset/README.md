# Changesets

This folder is managed by [Changesets](https://github.com/changesets/changesets). Run `pnpm changeset` to record an intent-to-release for any change that should appear in the next published version of `react-canvas-masker-auto-selection`.
